export interface UserType {
  firstName?: string;
  lastName?: string;
  email?: string;
  password?: string;
  userId?: string;
}

export interface loginInfo {
  email: string;
  password: string;
}
